#include <stdio.h>
#include <stdlib.h>

int main()
{
    int w = 67;
    printf(" weight is %d kg", w);
    printf(" weight is %2d kg", w);
    printf(" weight is %3d kg", w);
    printf(" weight is %4d kg", w);
    return 0;
}
